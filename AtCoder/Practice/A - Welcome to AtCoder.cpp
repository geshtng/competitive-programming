#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int a, b, c;
    string str;

    cin >> a >> b >> c;
    cin >> str;

    cout << a+b+c << " " << str << endl;

    return 0;
}