#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c; cin >> a >> b >> c;

    if (a < b && a < c) cout << "Monnei\n";
    else if (b < a && b < c) cout << "Fjee\n";
    else cout << "Dolladollabilljoll\n";

    return 0;
}
