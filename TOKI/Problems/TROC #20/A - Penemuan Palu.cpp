#include<bits/stdc++.h>
using namespace std;

int main (){
    ios::sync_with_stdio(false);
    cin.tie(0);

    int X, Y;

    cin >> X >> Y;
    
    string ans = (X > Y) ? "YES" : "NO";

    cout << ans << endl;

    return 0;
}
